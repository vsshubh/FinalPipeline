---
- name: Deploy to Tomcat
  hosts: web
  become: yes
  tasks:
    - name: Copy War File to Tomcat
        copy:
          src: "/var/lib/jenkins/workspace/FinalPipeline/target/MavenAnsibleWebApp.war"
          dest: "/opt/tomcat/MavenAnsibleWebApp.war"
          remote_src: no
